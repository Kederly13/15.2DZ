import classes from './styles.module.css';

const Header = () => (
    <div className="container">
        <header className={classes.header}>
            <p>Posts Page</p>
        </header>
    </div>
)
  
  export { Header };