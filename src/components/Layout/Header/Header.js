import classes from './styles.module.css'

const Header = () => {
    return (
        <header className={classes.mainHeader}>
            <p>header</p>
        </header>
    )
}
  
  export { Header };